{
    "name": "devemain",
    "version": "1.0.1",
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "i": "npm ci                                    # clean install",
        "u": "npm install                               # install/update",
        "p": "npm ci --only=production                  # production",
        "r": "semantic-release                          # version release",
        "r:test": "semantic-release --dry-run           # version test"
    },
    "devDependencies": {
        "@semantic-release/changelog": "^6.0.3",
        "@semantic-release/git": "^10.0.1",
        "@semantic-release/npm": "^12.0.2",
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "conventional-changelog-conventionalcommits": "^9.1.0",
        "laravel-vite-plugin": "^2.0.0",
        "semantic-release": "^24.2.9",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
